import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.wojohowitz.aiengineerguide',
  appName: 'AI Engineer Guide',
  webDir: 'dist/client'
};

export default config;
